from sympy import *

# a, b, c, d, e, f, g, h, i, j, k, l, m = symbols("a, b, c, d, e, f, g, h, i, j, k, l, m")
from sympy.abc import a,b,c,d,e,f

# def helpdot()

def ortogonalkompliment():
    print("Defination 10.5 bruges til at finde det ortogonale komplement.")
    print("Først udregnes det indre produkt. Dette vil give en ligning man kan sætte lig 0")
    print("Denne ligning laves om til et matrixrepresentation.")
    print("Fx: 1a+5b-3c -> (1 5 -3)")
    print("Nu kan det ortogonale komplement findes ved af finde nulrummet til denne matrice.")
    # size = V[0].shape[0]
    # if size == 1:
    #     v = Matrix([a])
    # if size == 2:
    #     v = Matrix([a], [b])
    # if size == 3:
    #     v = Matrix([a], [b], [c])
    # if size == 4:
    #     v = Matrix([a], [d], [c], [d])
    # if size == 5:
    #     v = Matrix([a], [b], [c], [d], [e])
    # if size == 5:
    #     v = Matrix([a], [b], [c], [d], [e], [f])
    # if size == 1:
    #     v = Matrix
    # if size == 2:
    #     v = Matrix([a], [b])
    # if size == 3:
    #     v = Matrix([a], [b], [c])
    # if size == 4:
    #     v = Matrix([a], [d], [c], [d])
    # if size == 5:
    #     v = Matrix([a], [b], [c], [d], [e])
    # if size == 5:
    #     v = Matrix([a], [b], [c], [d], [e], [f])
    # O = zeros(size, 1)
    # for x in V:
    #     print(x[1])


